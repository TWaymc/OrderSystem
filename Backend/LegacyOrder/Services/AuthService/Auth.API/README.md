

Note:
-   Very simple Auth API
-   Query not properly Validated (later if I have time)
-   Role and Permission not managed  (later if I have time)
-   Register Query is Authenticated by the gateway but not on this API
-   To Add Log Events for Errors   ( TODO later )

To Create The Firs User modify the ocelot GateWay setting to allow Register Query without a Token,
Or query directly the Auth API